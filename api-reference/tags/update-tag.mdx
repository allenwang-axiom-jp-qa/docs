---
title: '更新标签'
api: 'PUT https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/tag'
description: '更新指定标签的信息'
---

更新指定标签的信息

## 查询参数

<ParamField query="tag_id" type="string" required>
  标签ID
</ParamField>


## 请求体

<ParamField body="name" type="string">
  标签名称
</ParamField>

<ParamField body="identifier" type="string">
  标识符
</ParamField>

<ParamField body="tag_type" type="integer">
  标签类型
</ParamField>

<ParamField body="status" type="integer">
  状态
</ParamField>

<ParamField body="description" type="string">
  描述
</ParamField>


## 响应

<ResponseField name="message" type="string">
  字段说明
</ResponseField>

<RequestExample>

```bash cURL
curl -X PUT "https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/tag?tag_id={YOUR_TAG_ID}" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "key": "value"
  }'
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/tag?tag_id={YOUR_TAG_ID}', {
  method: 'PUT',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    key: 'value'
  })
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/tag?tag_id={YOUR_TAG_ID}'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}
data = {
    'key': 'value'
}

response = requests.put(url, headers=headers, json=data)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 更新成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
