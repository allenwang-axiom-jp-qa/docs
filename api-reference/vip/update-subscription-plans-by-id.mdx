---
title: '更新订阅套餐'
api: 'PUT https://dev-admin.beauty-666.com/subscription-plans/{id}'
description: '更新指定订阅套餐的信息'
---

更新指定订阅套餐的信息

## 请求头

<ParamField header="Authorization" type="string" required>
  JWT Bearer token，格式：`Bearer YOUR_TOKEN`
</ParamField>

## 路径参数

<ParamField path="id" type="string" required>
  套餐ID
</ParamField>


## 请求体

<ParamField body="name" type="string">
  字段说明，示例: `更新测试套餐`
</ParamField>

<ParamField body="price_cny_cents" type="integer">
  字段说明，示例: `3499`
</ParamField>


## 响应

<ResponseField name="code" type="integer">
  响应码
</ResponseField>

<ResponseField name="message" type="string">
  响应消息
</ResponseField>

<RequestExample>

```bash cURL
curl -X PUT "https://dev-admin.beauty-666.com/subscription-plans/{YOUR_ID}" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "key": "value"
  }'
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/subscription-plans/{YOUR_ID}', {
  method: 'PUT',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    key: 'value'
  })
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/subscription-plans/{YOUR_ID}'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}
data = {
    'key': 'value'
}

response = requests.put(url, headers=headers, json=data)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 更新成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
