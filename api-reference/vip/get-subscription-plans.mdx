---
title: '获取订阅套餐列表'
api: 'GET https://dev-admin.beauty-666.com/subscription-plans'
description: '获取所有订阅套餐的列表'
---

获取所有订阅套餐的列表

## 请求头

<ParamField header="Authorization" type="string" required>
  JWT Bearer token，格式：`Bearer YOUR_TOKEN`
</ParamField>

## 查询参数

<ParamField query="page" type="integer" default="1">
  页码
</ParamField>

<ParamField query="size" type="integer" default="20">
  每页数量
</ParamField>


## 响应

<ResponseField name="items" type="array<string>">
  数据列表
</ResponseField>

<ResponseField name="page" type="integer">
  当前页码
</ResponseField>

<ResponseField name="size" type="integer">
  每页数量
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://dev-admin.beauty-666.com/subscription-plans?page={YOUR_PAGE}&size={YOUR_SIZE}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/subscription-plans?page={YOUR_PAGE}&size={YOUR_SIZE}', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/subscription-plans?page={YOUR_PAGE}&size={YOUR_SIZE}'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}

response = requests.get(url, headers=headers)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 获取成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
