---
title: '获取单个订阅套餐'
api: 'GET https://dev-admin.beauty-666.com/subscription-plans/{id}'
description: '根据ID获取单个订阅套餐详情'
---

根据ID获取单个订阅套餐详情

## 请求头

<ParamField header="Authorization" type="string" required>
  JWT Bearer token，格式：`Bearer YOUR_TOKEN`
</ParamField>

## 路径参数

<ParamField path="id" type="string" required>
  套餐ID
</ParamField>


## 响应

<ResponseField name="id" type="integer">
  数据库自增ID
</ResponseField>

<ResponseField name="created_at" type="string">
  创建时间
</ResponseField>

<ResponseField name="updated_at" type="string">
  更新时间
</ResponseField>

<ResponseField name="deleted_at" type="string">
  删除时间
</ResponseField>

<ResponseField name="code" type="string">
  套餐编码
</ResponseField>

<ResponseField name="name" type="string">
  套餐名称
</ResponseField>

<ResponseField name="duration_days" type="integer">
  持续天数
</ResponseField>

<ResponseField name="duration_type" type="string">
  持续类型
</ResponseField>

<ResponseField name="price_cny_cents" type="integer">
  人民币价格（分）
</ResponseField>

<ResponseField name="original_price_cny_cents" type="integer">
  原价（分）
</ResponseField>

<ResponseField name="price_usdt_cents" type="integer">
  USDT价格（分）
</ResponseField>

<ResponseField name="status" type="string (active, inactive)">
  状态
</ResponseField>

<ResponseField name="meta" type="string">
  扩展字段（JSON）
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://dev-admin.beauty-666.com/subscription-plans/{YOUR_ID}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/subscription-plans/{YOUR_ID}', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/subscription-plans/{YOUR_ID}'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}

response = requests.get(url, headers=headers)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 获取成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```
```json 404 未找到
{
  "code": 404,
  "msg": "资源不存在"
}
```

</ResponseExample>
