---
title: '创建订阅套餐'
api: 'POST https://dev-admin.beauty-666.com/subscription-plans'
description: '创建一个新的订阅套餐'
---

创建一个新的订阅套餐

## 请求头

<ParamField header="Authorization" type="string" required>
  JWT Bearer token，格式：`Bearer YOUR_TOKEN`
</ParamField>

## 请求体

<ParamField body="code" type="string">
  字段说明，示例: `test_plan`
</ParamField>

<ParamField body="name" type="string">
  字段说明，示例: `测试套餐`
</ParamField>

<ParamField body="duration_days" type="integer">
  字段说明，示例: `30`
</ParamField>

<ParamField body="duration_type" type="string">
  字段说明，示例: `monthly`
</ParamField>

<ParamField body="price_cny_cents" type="integer">
  字段说明，示例: `2999`
</ParamField>

<ParamField body="original_price_cny_cents" type="integer">
  字段说明，示例: `3999`
</ParamField>

<ParamField body="price_usdt_cents" type="integer">
  字段说明，示例: `400`
</ParamField>

<ParamField body="status" type="string (active, inactive)">
  字段说明，示例: `active`
</ParamField>

<ParamField body="meta" type="string">
  字段说明，示例: `{"features": ["高清观看", "基础功能"]}`
</ParamField>


## 响应

<ResponseField name="id" type="integer">
  数据库自增ID
</ResponseField>

<ResponseField name="created_at" type="string">
  创建时间
</ResponseField>

<ResponseField name="updated_at" type="string">
  更新时间
</ResponseField>

<ResponseField name="deleted_at" type="string">
  删除时间
</ResponseField>

<ResponseField name="code" type="string">
  套餐编码
</ResponseField>

<ResponseField name="name" type="string">
  套餐名称
</ResponseField>

<ResponseField name="duration_days" type="integer">
  持续天数
</ResponseField>

<ResponseField name="duration_type" type="string">
  持续类型
</ResponseField>

<ResponseField name="price_cny_cents" type="integer">
  人民币价格（分）
</ResponseField>

<ResponseField name="original_price_cny_cents" type="integer">
  原价（分）
</ResponseField>

<ResponseField name="price_usdt_cents" type="integer">
  USDT价格（分）
</ResponseField>

<ResponseField name="status" type="string (active, inactive)">
  状态
</ResponseField>

<ResponseField name="meta" type="string">
  扩展字段（JSON）
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST "https://dev-admin.beauty-666.com/subscription-plans" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "key": "value"
  }'
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/subscription-plans', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    key: 'value'
  })
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/subscription-plans'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}
data = {
    'key': 'value'
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 201 创建成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
