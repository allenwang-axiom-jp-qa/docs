---
title: '修改提现密码'
api: 'PUT https://stg-api.beauty-666.com/api/v1/client/withdrawal/update'
description: '修改提现密码'
---

## 请求头

<ParamField header="Authorization" type="string" required>
  JWT Bearer token，格式：`Bearer YOUR_TOKEN`
</ParamField>


## 请求体

<ParamField body="old_password" type="string" required>
  Old Password
</ParamField>

<ParamField body="new_password" type="string" required>
  New Password
</ParamField>


## 响应

<ResponseField name="code" type="integer">
  响应状态码，0 表示成功
</ResponseField>

<ResponseField name="message" type="string">
  响应消息
</ResponseField>


<RequestExample>

```bash cURL
curl -X PUT 'https://stg-api.beauty-666.com/api/v1/client/withdrawal/update' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -H 'Content-Type: application/json'
```

```javascript JavaScript
const response = await fetch('https://stg-api.beauty-666.com/api/v1/client/withdrawal/update', {
  method: 'PUT',
  headers: {
    'Authorization': 'Bearer ' + token,
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
```

```python Python
import requests

response = requests.put(
    'https://stg-api.beauty-666.com/api/v1/client/withdrawal/update',
    headers={
        'Authorization': f'Bearer {token}',
        'Content-Type': 'application/json'
    }
)
data = response.json()
```

</RequestExample>

<ResponseExample>

```json 成功响应
{
  "code": 0,
  "message": "success",
  "data": {
    ...
  }
}
```

```json 错误响应
{
  "code": 400,
  "message": "错误信息"
}
```

</ResponseExample>
