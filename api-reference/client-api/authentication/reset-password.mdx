---
title: '重置密码'
api: 'PUT https://stg-api.beauty-666.com/api/v1/client/reset_password'
description: '通过验证码重置密码（忘记密码）'
---


## 请求体

<ParamField body="account" type="string" required>
  账号
  示例：`user123`
</ParamField>

<ParamField body="new_password" type="string" required>
  新密码
  示例：`NewPwd123`
</ParamField>

<ParamField body="verification_code" type="string" required>
  验证 验证码
  示例：`123456`
</ParamField>


## 响应

<ResponseField name="code" type="integer">
  响应状态码，0 表示成功
</ResponseField>

<ResponseField name="message" type="string">
  响应消息
</ResponseField>


<RequestExample>

```bash cURL
curl -X PUT 'https://stg-api.beauty-666.com/api/v1/client/reset_password' \
  -H 'Content-Type: application/json'
```

```javascript JavaScript
const response = await fetch('https://stg-api.beauty-666.com/api/v1/client/reset_password', {
  method: 'PUT',
  headers: {
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
```

```python Python
import requests

response = requests.put(
    'https://stg-api.beauty-666.com/api/v1/client/reset_password',
    headers={
        'Content-Type': 'application/json'
    }
)
data = response.json()
```

</RequestExample>

<ResponseExample>

```json 成功响应
{
  "code": 0,
  "message": "success",
  "data": {
    ...
  }
}
```

```json 错误响应
{
  "code": 400,
  "message": "错误信息"
}
```

</ResponseExample>
