---
title: '获取订单列表'
api: 'GET https://stg-api.beauty-666.com/api/v1/client/purchase/orders'
description: '获取用户的订单列表'
---

## 请求头

<ParamField header="Authorization" type="string" required>
  JWT Bearer token，格式：`Bearer YOUR_TOKEN`
</ParamField>


## 查询参数

<ParamField query="page" type="integer" default="1">
  页码
</ParamField>

<ParamField query="page_size" type="integer" default="20">
  每页数量
</ParamField>

<ParamField query="order_type" type="string">
  订单类型
</ParamField>

<ParamField query="status" type="string">
  订单状态
</ParamField>


## 响应

<ResponseField name="code" type="integer">
  响应状态码，0 表示成功
</ResponseField>

<ResponseField name="message" type="string">
  响应消息
</ResponseField>

<ResponseField name="data" type="object">
  响应数据
</ResponseField>


<RequestExample>

```bash cURL
curl -X GET 'https://stg-api.beauty-666.com/api/v1/client/purchase/orders' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```

```javascript JavaScript
const response = await fetch('https://stg-api.beauty-666.com/api/v1/client/purchase/orders', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer ' + token,
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
```

```python Python
import requests

response = requests.get(
    'https://stg-api.beauty-666.com/api/v1/client/purchase/orders',
    headers={
        'Authorization': f'Bearer {token}',
        'Content-Type': 'application/json'
    }
)
data = response.json()
```

</RequestExample>

<ResponseExample>

```json 成功响应
{
  "code": 0,
  "message": "success",
  "data": {
    ...
  }
}
```

```json 错误响应
{
  "code": 400,
  "message": "错误信息"
}
```

</ResponseExample>
