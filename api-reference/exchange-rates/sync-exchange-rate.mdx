---
title: '同步汇率'
api: 'POST https://dev-admin.beauty-666.com/api/v1/oms/exchange-rates/{id}/sync'
description: '从外部 API 同步指定币种的汇率'
---

手动触发从外部 API 同步指定币种的汇率。目前支持 USDT 通过 CoinGecko API 同步。

## 请求头

<ParamField header="Authorization" type="string" required>
  JWT Bearer token，格式：`Bearer YOUR_TOKEN`
</ParamField>

## 路径参数

<ParamField path="id" type="integer" required>
  币种 ID
</ParamField>

## 响应

<ResponseField name="code" type="integer">
  响应状态码，0 表示成功，7 表示业务错误
</ResponseField>

<ResponseField name="data" type="object">
  同步后的币种对象

  <Expandable title="币种对象">
    <ResponseField name="id" type="integer">
      币种 ID
    </ResponseField>
    <ResponseField name="code" type="string">
      币种代码
    </ResponseField>
    <ResponseField name="name" type="string">
      币种名称
    </ResponseField>
    <ResponseField name="symbol" type="string">
      货币符号
    </ResponseField>
    <ResponseField name="currency_type" type="integer">
      币种类型
    </ResponseField>
    <ResponseField name="exchange_rate" type="string">
      同步后的新汇率
    </ResponseField>
    <ResponseField name="update_mode" type="integer">
      更新模式
    </ResponseField>
    <ResponseField name="last_updated_at" type="string">
      更新时间（同步后自动更新）
    </ResponseField>
    <ResponseField name="status" type="integer">
      状态
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="msg" type="string">
  响应消息
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST "https://dev-admin.beauty-666.com/api/v1/oms/exchange-rates/10/sync" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```javascript JavaScript
const currencyId = 10;
const response = await fetch(
  `https://dev-admin.beauty-666.com/api/v1/oms/exchange-rates/${currencyId}/sync`,
  {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer YOUR_TOKEN'
    }
  }
);
const data = await response.json();

if (data.code === 0) {
  console.log(`汇率已更新: ${data.data.exchange_rate}`);
}
```

```python Python
import requests

currency_id = 10
response = requests.post(
    f'https://dev-admin.beauty-666.com/api/v1/oms/exchange-rates/{currency_id}/sync',
    headers={'Authorization': 'Bearer YOUR_TOKEN'}
)
data = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 同步成功
{
  "code": 0,
  "data": {
    "id": 10,
    "code": "USDT",
    "name": "泰达币",
    "symbol": "₮",
    "currency_type": 1,
    "exchange_rate": "7.18",
    "update_mode": 1,
    "last_updated_at": "2026-01-14T19:45:00+09:00",
    "status": 1
  },
  "msg": "同步成功"
}
```

```json 7 不支持同步
{
  "code": 7,
  "data": {},
  "msg": "同步汇率失败: 暂不支持该加密货币自动同步"
}
```

```json 7 基准货币
{
  "code": 7,
  "data": {},
  "msg": "同步汇率失败: 基准货币(CNY)无需同步"
}
```

```json 7 币种不存在
{
  "code": 7,
  "data": {},
  "msg": "币种不存在"
}
```

</ResponseExample>

<Info>
  **同步数据源**

  | 币种类型 | 数据源 | 状态 |
  |---------|-------|------|
  | USDT | CoinGecko API | ✅ 已支持 |
  | 其他加密货币 | CoinGecko API | 🚧 规划中 |
  | 法币 | ExchangeRate-API | 🚧 规划中 |
</Info>

<Warning>
  **API 调用限制**

  CoinGecko 免费 API 有调用频率限制，请勿频繁调用同步接口。建议通过定时任务进行自动同步。
</Warning>
