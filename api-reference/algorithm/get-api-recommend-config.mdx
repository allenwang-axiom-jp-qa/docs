---
title: '获取推荐配置'
api: 'GET https://dev-admin.beauty-666.com/api/recommend/config'
description: '获取推荐算法的权重配置'
---

获取推荐算法的权重配置

## 请求头

<ParamField header="Authorization" type="string" required>
  JWT Bearer token，格式：`Bearer YOUR_TOKEN`
</ParamField>

## 响应

<ResponseField name="key" type="string">
  配置键
</ResponseField>

<ResponseField name="value" type="string">
  配置值
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://dev-admin.beauty-666.com/api/recommend/config" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/api/recommend/config', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/api/recommend/config'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}

response = requests.get(url, headers=headers)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 获取成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
