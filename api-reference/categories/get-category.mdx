---
title: '获取单个分类'
api: 'GET https://dev-admin.beauty-666.com/api/v1/category'
description: '根据ID获取单个分类详情'
---

根据ID获取单个分类详情

## 查询参数

<ParamField query="category_id" type="string" required>
  分类ID
</ParamField>


## 响应

<ResponseField name="id" type="integer">
  分类ID
</ResponseField>

<ResponseField name="category_id" type="string">
  分类ID字符串
</ResponseField>

<ResponseField name="name" type="string">
  分类名称
</ResponseField>

<ResponseField name="parent_id" type="integer">
  父分类ID
</ResponseField>

<ResponseField name="sort_order" type="integer">
  排序
</ResponseField>

<ResponseField name="status" type="integer">
  状态
</ResponseField>

<ResponseField name="description" type="string">
  描述
</ResponseField>

<ResponseField name="video_count" type="integer">
  视频数量
</ResponseField>

<ResponseField name="created_time" type="integer">
  创建时间
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://dev-admin.beauty-666.com/api/v1/category?category_id={YOUR_CATEGORY_ID}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/api/v1/category?category_id={YOUR_CATEGORY_ID}', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/api/v1/category?category_id={YOUR_CATEGORY_ID}'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}

response = requests.get(url, headers=headers)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 获取成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
