---
title: '获取分类路径'
api: 'GET https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/tags'
description: '获取分类的完整路径（面包屑）'
---

获取分类的完整路径（面包屑）

## 查询参数

<ParamField query="category_id" type="string" required>
  分类ID
</ParamField>


## 响应

<ResponseField name="path" type="array<object>">
  路径数组
</ResponseField>

<ResponseField name="level" type="integer">
  层级
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/tags?category_id={YOUR_CATEGORY_ID}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/tags?category_id={YOUR_CATEGORY_ID}', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/tags?category_id={YOUR_CATEGORY_ID}'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}

response = requests.get(url, headers=headers)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 获取成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
