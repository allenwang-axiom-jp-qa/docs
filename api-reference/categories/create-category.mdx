---
title: '创建分类'
api: 'POST https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/category'
description: '创建一个新的分类'
---

创建一个新的分类

## 请求体

<ParamField body="name" type="string" required>
  分类名称
</ParamField>

<ParamField body="parent_id" type="integer">
  父分类ID
</ParamField>

<ParamField body="sort_order" type="integer">
  排序
</ParamField>

<ParamField body="status" type="integer">
  状态
</ParamField>

<ParamField body="description" type="string">
  描述
</ParamField>


## 响应

<ResponseField name="category_id" type="integer">
  分类ID
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST "https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/category" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "key": "value"
  }'
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/category', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    key: 'value'
  })
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/category'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}
data = {
    'key': 'value'
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 201 创建成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
