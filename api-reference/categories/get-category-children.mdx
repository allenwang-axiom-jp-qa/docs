---
title: '获取分类子项'
api: 'GET https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/category/children'
description: '获取分类的子项，支持递归获取'
---

获取分类的子项，支持递归获取

## 查询参数

<ParamField query="category_id" type="string" required>
  分类ID
</ParamField>

<ParamField query="include_descendants" type="boolean" default="False">
  是否包含后代
</ParamField>

<ParamField query="status" type="integer">
  状态筛选
</ParamField>


## 响应

<ResponseField name="code" type="integer">
  状态码，0 表示成功
</ResponseField>

<ResponseField name="msg" type="string">
  响应消息
</ResponseField>

<ResponseField name="data" type="object">
  响应数据
</ResponseField>


<RequestExample>

```bash cURL
curl -X GET "https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/category/children?category_id={YOUR_CATEGORY_ID}&include_descendants={YOUR_INCLUDE_DESCENDANTS}&status={YOUR_STATUS}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/category/children?category_id={YOUR_CATEGORY_ID}&include_descendants={YOUR_INCLUDE_DESCENDANTS}&status={YOUR_STATUS}', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/category/children?category_id={YOUR_CATEGORY_ID}&include_descendants={YOUR_INCLUDE_DESCENDANTS}&status={YOUR_STATUS}'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}

response = requests.get(url, headers=headers)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 获取成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
