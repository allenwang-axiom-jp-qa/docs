---
title: '获取分类列表'
api: 'GET https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/categories'
description: '分页获取分类列表'
---

分页获取分类列表

## 查询参数

<ParamField query="page" type="integer" default="1">
  页码
</ParamField>

<ParamField query="page_size" type="integer" default="20">
  每页数量
</ParamField>

<ParamField query="parent_id" type="integer">
  父分类ID
</ParamField>

<ParamField query="status" type="integer">
  状态
</ParamField>


## 响应

<ResponseField name="total" type="integer">
  总数
</ResponseField>

<ResponseField name="page" type="integer">
  页码
</ResponseField>

<ResponseField name="list" type="array<object>">
  分类列表
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/categories?page={YOUR_PAGE}&page_size={YOUR_PAGE_SIZE}&parent_id={YOUR_PARENT_ID}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```javascript JavaScript
const response = await fetch('https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/categories?page={YOUR_PAGE}&page_size={YOUR_PAGE_SIZE}&parent_id={YOUR_PARENT_ID}', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  }
});

const data = await response.json();
```

```python Python
import requests

url = 'https://dev-admin.beauty-666.com/api/v1/site-svc-video-mgmt/categories?page={YOUR_PAGE}&page_size={YOUR_PAGE_SIZE}&parent_id={YOUR_PARENT_ID}'
headers = {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
}

response = requests.get(url, headers=headers)
result = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 获取成功
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```
```json 401 未授权
{
  "code": 401,
  "msg": "未授权，请先登录"
}
```

</ResponseExample>
